apiVersion: v1
kind: Service
metadata:
  name: ga-controller-headless
  namespace: default
  labels:
    app: ga-controller
spec:
  publishNotReadyAddresses: true
  clusterIP: None 
  selector:
    app: ga-controller
  ports:
    - name: http
      port: 8000
      targetPort: 8000
